{
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build active file",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "${fileDirname}/main.cpp",
        "${fileDirname}/image.cpp",
        "${fileDirname}/bitmap.cpp",
        "${fileDirname}/http.cpp",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}",
        "-lSDL2",
        "-lpthread"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}